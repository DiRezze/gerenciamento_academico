import React from "react";

const LoginPage = () => {
    return(
        <main>
            <form>
                <input type="email" />
                <input type="password" />
                <button type="submit">Acessar</button>
            </form>
        </main>
    );
}

export default LoginPage;